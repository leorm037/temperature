{% extends 'base.html.twig' %}

{% block body %}
    <div class="card mb-3 pb-3" sty{{ 'http_error_404.description'|trans }}le="max-width: 18rem;">
        <div class="card-header">{{ 'http_error.name'|trans({ 'status_code': 404 }) }}</div>
        <div class="card-body text-center">
            <img src="{{ asset('img/error/404.svg') }}" width="200" class="m-3">

            <h5 class="card-title">{{ 'http_error_404.description'|trans }}</h5>
            <p class="card-text">{{ 'http_error_404.suggestion'|trans({ 'url': url('app_index') })|raw }}</p>
        </div>
    </div>
{% endblock %}
